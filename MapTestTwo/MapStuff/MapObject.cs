using System;
using System.Collections.Generic;
using System.Text;

namespace MapTestTwo.MapStuff
{
    class MapObject
    {
        int Position;
        string Icon;
        string MapType;

        // each MapObject has a postion 
        // each MapObject has a Image to store
        // each MapObject has a type of thing that it is, which dictates how it acts

        // stuff like if postion is [ 1, 1 ] remove what image is there now with this icon, and act based off of type?


        
    }
}
